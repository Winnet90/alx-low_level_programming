#include "main.h"
 /**
  * function _abs - function that prints the absolute value of an int
  * @n: int in question
  *
  * Return: abs int
  */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
